<header>
    <nav class="navbar navbar-expand-md navbar-dark fixed-top bg-dark" style="justify-content:space-between;">
        <a class="navbar-brand" href="http://127.0.0.1:8000/">HaberNews</a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarCollapse" aria-controls="navbarCollapse" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <ul class="navbar-nav mb-2 mb-md-0">
            <li class="nav-item">
                <a class="nav-link active" aria-current="page" href="#">AnaSayfa</a>
            </li>
            <li class="nav-item">
                <a class="nav-link" href="/allnews">Haberler</a>
            </li>
            <li class="nav-item">
                <a class="nav-link" href="#" tabindex="-1" >kategoriler</a>
            </li>
        </ul>
        <form class="d-flex ">
            <input class="form-control me-2" type="search" placeholder="Search" aria-label="Search">
            <button class="btn btn-outline-success" type="submit">Search</button>

        </form>
            &nbsp;
                {% if is_granted("IS_AUTHENTICATED_REMEMBERED") %}
                    <div class="float-right border p-2 pt-0" style="margin-right: 5%; text-align: center">

                    <p class="navbar-text">
                        <a href="/profile" style="text-decoration:none" >
                    {{ 'layout.logged_in_as'|trans({'%username%': app.user.username}, 'FOSUserBundle') }} </a></p>|
                    <a class="btn btn-primary" href="{{ path('fos_user_security_logout') }}">
                        {{ 'layout.logout'|trans({}, 'FOSUserBundle') }}
                    <i class="fas fa-sign-out-alt"></i>
                    </a>
            </div>
                {% else %}
        <div class="float-right p-2 pt-0" style="margin-right: 5%; text-align: center">

        <a class="btn btn-outline-primary" href="{{ path('fos_user_security_login') }}">{{ 'layout.login'|trans({}, 'FOSUserBundle') }}</a>
        </div>
                {% endif %}


    </nav>
</header>